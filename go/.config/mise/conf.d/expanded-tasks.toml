# Code generated by tasks:expand task. DO NOT EDIT.

[tasks."hello_sh:gen"]
description='''Generate Go-inlined sample scripts.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_hello_sh__gen
_APPDIR="$MISE_ORIGINAL_CWD"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_ORIGINAL_CWD"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_hello_sh__gen "$@"
'''

[tasks."gen"]
description='''Generate files.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_gen
_APPDIR="$MISE_ORIGINAL_CWD"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_ORIGINAL_CWD"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_gen "$@"
'''

[tasks."depbuild"]
description='''Build Go source packages (*.go, ./cmd/*/) incrementally.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_depbuild
_APPDIR="$MISE_ORIGINAL_CWD"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_ORIGINAL_CWD"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_depbuild "$@"
'''

[tasks."build"]
description='''Build'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_build
_APPDIR="$MISE_ORIGINAL_CWD"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_ORIGINAL_CWD"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_build "$@"
'''

[tasks."install"]
description='''Install Go tools.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_install
_APPDIR="$MISE_ORIGINAL_CWD"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_ORIGINAL_CWD"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_install "$@"
'''
