# Code generated by tasks:expand task. DO NOT EDIT.

[tasks."hello_sh:gen"]
description='''Generate Go-inlined sample scripts.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_hello_sh__gen
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_hello_sh__gen "$@"
'''

[tasks."gen"]
description='''Generate files.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_gen
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_gen "$@"
'''

[tasks."depbuild"]
description='''Build Go source packages (*.go, ./cmd/*/) incrementally.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_depbuild
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_depbuild "$@"
'''

[tasks."build"]
description='''Build'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_build
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_build "$@"
'''

[tasks."install"]
description='''Install Go tools.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_install
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_install "$@"
'''
