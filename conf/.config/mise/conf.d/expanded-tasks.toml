# Code generated by tasks:expand task. DO NOT EDIT.

[tasks."dotfiles:install"]
description='''Install dotfiles to $HOME/'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_dotfiles__install
_APPDIR="$MISE_PROJECT_ROOT"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_dotfiles__install "$@"
'''

[tasks."karabiner_json:update"]
description='''Update karabiner.json'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_karabiner_json__update
_APPDIR="$MISE_PROJECT_ROOT"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_karabiner_json__update "$@"
'''

[tasks."karabiner_json:install"]
description='''Install karabiner.json to $HOME/.config/karabiner as hard link'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_karabiner_json__install
_APPDIR="$MISE_PROJECT_ROOT"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_karabiner_json__install "$@"
'''

[tasks."ps1:install"]
description='''Install PS1 profile.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_ps1__install
_APPDIR="$MISE_PROJECT_ROOT"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_ps1__install "$@"
'''

[tasks."install"]
description='''Install configuration files.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_install
_APPDIR="$MISE_PROJECT_ROOT"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_install "$@"
'''

[tasks."gemini:gen"]
description='''Generate Gemini stuff from Claude configurations.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-project.sh:task_gemini__gen
_APPDIR="$MISE_PROJECT_ROOT"/".mise"
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-project.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_gemini__gen "$@"
'''
