# Code generated by tasks:expand task. DO NOT EDIT.

[tasks."bcrypt:hash"]
description='''[password] Create hash from password with bcrypt.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-bcrypt.sh:task_bcrypt__hash
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-bcrypt.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_bcrypt__hash "$@"
'''
tools={"uv"="0.10"}

[tasks."bcrypt:verify"]
description='''[--password=<password> --hash=<hash>] Verify password against bcrypt hash.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-bcrypt.sh:task_bcrypt__verify
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-bcrypt.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_bcrypt__verify "$@"
'''
tools={"uv"="0.10"}

[tasks."docker:status"]
description='''Show the status of Docker.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__status
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-docker.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__status "$@"
'''

[tasks."docker:start"]
description='''Start Docker.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__start
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-docker.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__start "$@"
'''

[tasks."docker:stop"]
description='''Stop Docker.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__stop
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-docker.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__stop "$@"
'''

[tasks."docker:start:temp"]
description='''Start Docker service temporarily. If Docker is already running, do nothing when the task runner exits.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__start__temp
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-docker.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__start__temp "$@"
'''
hide=true

[tasks."docker:debian:run"]
description='''Run Debian Docker container.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__debian__run
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-docker.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__debian__run "$@"
'''

[tasks."docker:busybox:run"]
description='''Run BusyBox Docker container.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__busybox__run
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-docker.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__busybox__run "$@"
'''

[tasks."docker:archlinux:run"]
description='''Run Arch Linux Docker container.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__archlinux__run
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-docker.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__archlinux__run "$@"
'''

[tasks."session:init"]
description='''Clears the session environment.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-session.sh:task_session__init
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-session.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_session__init "$@"
'''
hide=true

[tasks."appenv:ensure"]
description='''Ensures the application environment variable $APP_ENV is set.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-session.sh:task_appenv__ensure
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-session.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_appenv__ensure "$@"
'''
hide=true
depends=["session:init"]
tools={"gum"="0.17.0"}

[tasks."critical:confirm"]
description='''Confirms a critical operation.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-session.sh:task_critical__confirm
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-session.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_critical__confirm "$@"
'''
hide=true
depends=["appenv:ensure"]

[tasks."test"]
description='''[tests...] Run tests. If no test names are provided, all tests are run.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-test.sh:task_test
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-test.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_test "$@"
'''

[tasks."npm:install"]
description='''Install npm packages if "package.json" has been updated.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-node.bash:task_npm__install
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-node.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_npm__install "$@"
'''

[tasks."foo"]
description='''Foo'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_foo
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_foo "$@"
'''

[tasks."doc"]
description='''Update documentation files.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_doc
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_doc "$@"
'''
