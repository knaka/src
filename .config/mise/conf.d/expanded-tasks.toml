# Code generated by tasks:expand task. DO NOT EDIT.

[tasks."bcrypt:hash"]
description='''[password] Create hash from password with bcrypt.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-bcrypt.sh:task_bcrypt__hash
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-bcrypt.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_bcrypt__hash "$@"
'''
tools={"uv"="0.10"}

[tasks."bcrypt:verify"]
description='''[--password=<password> --hash=<hash>] Verify password against bcrypt hash.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-bcrypt.sh:task_bcrypt__verify
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-bcrypt.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_bcrypt__verify "$@"
'''
tools={"uv"="0.10"}

[tasks."docker:status"]
description='''Show the status of Docker.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__status
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-docker.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__status "$@"
'''

[tasks."docker:start"]
description='''Start Docker.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__start
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-docker.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__start "$@"
'''

[tasks."docker:stop"]
description='''Stop Docker.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__stop
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-docker.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__stop "$@"
'''

[tasks."docker:start:temp"]
description='''Start Docker service temporarily. If Docker is already running, do nothing when the task runner exits.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__start__temp
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-docker.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__start__temp "$@"
'''
hide=true

[tasks."docker:debian:run"]
description='''Run Debian Docker container.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__debian__run
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-docker.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__debian__run "$@"
'''

[tasks."docker:busybox:run"]
description='''Run BusyBox Docker container.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-docker.sh:task_docker__busybox__run
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-docker.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_docker__busybox__run "$@"
'''

[tasks."hooks:log:test"]
description=''''''
run='''
#!/usr/bin/env sh
# .mise/tasks-hooks.sh:task_hooks__log__test
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-hooks.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_hooks__log__test "$@"
'''
hide=true

[tasks."hooks:log"]
description=''''''
run='''
#!/usr/bin/env sh
# .mise/tasks-hooks.sh:task_hooks__log
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-hooks.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_hooks__log "$@"
'''
hide=true

[tasks."session:init"]
description='''Clears the session environment.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-session.sh:task_session__init
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-session.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_session__init "$@"
'''
hide=true

[tasks."appenv:ensure"]
description='''Ensures the application environment variable $APP_ENV is set.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-session.sh:task_appenv__ensure
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-session.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_appenv__ensure "$@"
'''
hide=true
depends=["session:init"]
tools={"gum"="0.17.0"}

[tasks."critical:confirm"]
description='''Confirms a critical operation.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-session.sh:task_critical__confirm
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-session.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_critical__confirm "$@"
'''
hide=true
depends=["appenv:ensure"]

[tasks."test"]
description='''[names...] Run tests. If no test names are provided, all tests are run.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-test.sh:task_test
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
. ./"tasks-test.sh"
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_test "$@"
'''

[tasks."bash:foo"]
description='''Bash foo.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-new.bash:task_bash__foo
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-new.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_bash__foo "$@"
'''
hide=true

[tasks."bash:bar"]
description='''Bash bar.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-new.bash:task-bash--bar
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-new.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task-bash--bar "$@"
'''

[tasks."baz"]
description='''Baz task.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-new.bash:baz
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-new.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
baz "$@"
'''

[tasks."bash:baz"]
description='''Bash baz.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-new.bash:bash_baz
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-new.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
bash_baz "$@"
'''

[tasks."sub:gen"]
description=''''''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_sub__gen
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_sub__gen "$@"
'''

[tasks."gen"]
description=''''''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_gen
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_gen "$@"
'''
