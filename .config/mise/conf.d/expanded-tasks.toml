# Code generated by tasks:expand task. DO NOT EDIT.

[tasks."session:init"]
description='''Clears the session environment.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-lib-session.sh:task_session__init
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-lib-session.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_session__init "$@"
'''
hide=true

[tasks."appenv:ensure"]
description='''Ensures the application environment variable $APP_ENV is set.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-lib-session.sh:task_appenv__ensure
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-lib-session.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_appenv__ensure "$@"
'''
hide=true
depends=["session:init"]
tools={"gum"="0.17.0"}

[tasks."critical:confirm"]
description='''Confirms a critical operation.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-lib-session.sh:task_critical__confirm
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-lib-session.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_critical__confirm "$@"
'''
hide=true
depends=["appenv:ensure"]

[tasks."test"]
description='''[tests...] Run tests. If no test names are provided, all tests are run.'''
run='''
#!/usr/bin/env sh
# .mise/tasks-lib-test.sh:task_test
saved_pwd_85053ae="$PWD"
cd "$MISE_PROJECT_ROOT"/".mise" || exit 1
set -- _SCRDIR "$PWD" "$@"
. ./"tasks-lib-test.sh"
shift 2
cd "$saved_pwd_85053ae" || exit 1
set -o nounset -o errexit
task_test "$@"
'''

[tasks."foo"]
description='''Foo'''
run='''
#!/usr/bin/env bash
# .mise/tasks.bash:task_foo
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_foo "$@"
'''

[tasks."doc"]
description='''Update documentation files.'''
run='''
#!/usr/bin/env bash
# .mise/tasks.bash:task_doc
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_doc "$@"
'''

[tasks."docker:debian:run"]
description='''Run Debian Docker container.'''
run='''
#!/usr/bin/env bash
# .mise/tasks.bash:task_docker__debian__run
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_docker__debian__run "$@"
'''

[tasks."docker:busybox:run"]
description='''Run BusyBox Docker container.'''
run='''
#!/usr/bin/env bash
# .mise/tasks.bash:task_docker__busybox__run
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_docker__busybox__run "$@"
'''

[tasks."docker:archlinux:run"]
description='''Run Arch Linux Docker container.'''
run='''
#!/usr/bin/env bash
# .mise/tasks.bash:task_docker__archlinux__run
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_docker__archlinux__run "$@"
'''

[tasks."bcrypt:hash"]
description='''[password] Create hash from password with bcrypt.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-lib-bcrypt.bash:task_bcrypt__hash
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-lib-bcrypt.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_bcrypt__hash "$@"
'''
tools={"uv"="0.10"}

[tasks."bcrypt:verify"]
description='''[--password=<password> --hash=<hash>] Verify password against bcrypt hash.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-lib-bcrypt.bash:task_bcrypt__verify
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-lib-bcrypt.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_bcrypt__verify "$@"
'''
tools={"uv"="0.10"}

[tasks."docker:status"]
description='''Show the status of Docker.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-lib-docker.bash:task_docker__status
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-lib-docker.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_docker__status "$@"
'''

[tasks."docker:start"]
description='''Start Docker.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-lib-docker.bash:task_docker__start
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-lib-docker.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_docker__start "$@"
'''

[tasks."docker:stop"]
description='''Stop Docker.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-lib-docker.bash:task_docker__stop
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-lib-docker.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_docker__stop "$@"
'''

[tasks."docker:start:temp"]
description='''Start Docker service temporarily. If Docker is already running, do nothing when the task runner exits.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-lib-docker.bash:task_docker__start__temp
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-lib-docker.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_docker__start__temp "$@"
'''
hide=true

[tasks."npm:install"]
description='''Install npm packages if "package.json" has been updated.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-lib-node.bash:task_npm__install
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-lib-node.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
TAIL_DEPTH=1 task_npm__install "$@"
'''
