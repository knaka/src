# Code generated by tasks:expand task. DO NOT EDIT.

[tasks."install"]
description='''Install scripts.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_install
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_install "$@"
'''

[tasks."run"]
description='''Run a script.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_run
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_run "$@"
'''

[tasks."sync"]
description='''Updates the UV virtualenv.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_sync
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_sync "$@"
'''

[tasks."foo"]
description='''Foo'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_foo
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_foo "$@"
'''
